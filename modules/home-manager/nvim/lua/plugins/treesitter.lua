return {
  {
    "nvim-treesitter/nvim-treesitter",
    opts = {
      -- Stop Neovim from trying to auto-install parsers at runtime
      ensure_installed = {},
      auto_install = false,
    },
  },
}

